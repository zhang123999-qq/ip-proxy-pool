# IP 代理池项目方案

> 基于 Python 搭建的免费代理 IP 池，采用实时打分制自动维护代理质量。

---

## 一、项目目标

- 自动爬取多个免费代理源，采集可用代理 IP
- 通过实时打分机制动态评估代理质量
- 自动淘汰低质量代理，保留高可用代理
- 提供简单 API/接口供业务方调用

---

## 二、核心需求

### 1. 代理采集
- 支持多个免费代理源（西刺、快代理、66ip、站大爷等）
- 多线程/异步并发采集，提高效率
- 自动去重，避免重复入库
- 定时增量更新

### 2. 代理验证
- 使用目标测试网站（如 httpbin.org、百度、知乎等）验证代理连通性
- 检测响应时间、匿名度、协议类型（HTTP/HTTPS）
- 验证超时控制（建议 5-10 秒）

### 3. 实时打分制 ⭐（核心机制）

| 事件 | 分数变化 |
|------|---------|
| 初始分数 | 10 分 |
| 验证可用 | +1 分 |
| 验证失败 | -3 分 |
| 业务使用成功 | +1 分 |
| 业务使用失败 | -3 分 |
| 分数 ≤ 0 | **自动从池中删除** |

### 4. 存储与调度
- 内存存储（dict/Redis）保证高性能读写
- 支持持久化（SQLite/JSON 文件）防止重启丢失
- 随机/权重轮询策略分配代理

### 5. API 服务
- 提供 RESTful API（FastAPI/Flask）
- 常用接口：获取代理、删除代理、查看池状态

---

## 三、技术选型

| 类别 | 选型 | 理由 |
|------|------|------|
| 语言 | Python 3.10+ | 生态成熟，爬虫首选 |
| HTTP 请求 | httpx / aiohttp | 异步支持好 |
| 网页解析 | BeautifulSoup4 + lxml | 简单高效 |
| Web 框架 | FastAPI | 高性能 + 自动文档 |
| 任务调度 | APScheduler | 轻量级定时任务 |
| 数据存储 | Redis / 内存 | 高速读写；备选 SQLite |
| 异步框架 | asyncio | 提升并发性能 |

---

## 四、系统架构

```
┌──────────────┐
│  代理源网站   │  ← 西刺/快代理/66ip 等
└──────┬───────┘
       │ 爬取
       ▼
┌──────────────┐
│  采集模块     │  Crawler (多源并发)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  验证模块     │  Validator (异步验证)
└──────┬───────┘
       │ 初始 10 分入库
       ▼
┌──────────────────┐
│  代理池 (Storage) │  ← Redis/Memory
│  {ip:port: score}│
└──────┬───────────┘
       │ 业务调用 + 反馈
       ▼
┌──────────────┐
│  打分引擎     │  +1 / -3
└──────┬───────┘
       │ 分数 ≤ 0 删除
       ▼
┌──────────────┐
│  API 服务     │  FastAPI
└──────────────┘
```

---

## 五、目录结构

```
D:\ip-proxy-pool\
├── README.md                 # 项目说明 + API 文档
├── requirements.txt          # 依赖清单
├── config.py                 # 全局配置
├── main.py                   # 入口（uvicorn）
├── .gitignore
├── crawler/                  # 采集模块
│   ├── __init__.py           # 默认管理器 + BUILTIN_CRAWLERS 导出
│   ├── base.py               # 爬虫抽象基类
│   ├── manager.py            # 多源并发调度
│   └── sources.py            # 内置免费源（快代理 / 66ip）
├── validator/                # 验证模块
│   ├── __init__.py
│   └── checker.py            # 异步验证 + 打分回调
├── pool/                     # 代理池核心
│   ├── __init__.py
│   └── core.py               # ProxyPool 单例 + 原子持久化
├── models/                   # 数据模型
│   ├── __init__.py
│   └── proxy.py              # ProxyItem + ProxyProtocol
├── api/                      # API 服务
│   ├── __init__.py
│   ├── app.py                # FastAPI 工厂 + lifespan
│   ├── routes.py             # 全部路由
│   └── schemas.py            # Pydantic 模型
├── scheduler/                # 调度器
│   ├── __init__.py
│   └── jobs.py               # 三周期任务
├── utils/                    # 工具
│   ├── __init__.py
│   └── logger.py             # loguru
├── data/                     # 持久化（运行时生成）
│   └── proxy_pool.json
├── logs/                     # 日志（运行时生成）
├── tests/                    # 单元测试
│   ├── test_pool.py
│   └── test_scorer.py
└── docs/
    ├── 需求文档.md
    └── 开发方案.md
```

---

## 六、开发阶段

### 阶段一：基础搭建（1-2 天）
- 项目结构初始化
- 代理存储模块（内存版）
- 基础验证功能

### 阶段二：采集模块（2-3 天）
- 实现 2-3 个免费代理源爬虫
- 异步并发采集
- 数据清洗与去重

### 阶段三：打分系统（1 天）
- 打分引擎实现
- 分数阈值自动删除
- 持久化存储

### 阶段四：API 与调度（1-2 天）
- FastAPI 接口开发
- APScheduler 定时任务
- 健康检查与监控

### 阶段五：测试与优化（1-2 天）
- 集成测试
- 性能调优
- 文档完善

---

## 七、风险与应对

| 风险 | 应对策略 |
|------|---------|
| 免费代理质量差、失效快 | 严格打分制，快速淘汰低质代理 |
| 代理源网站反爬 | 随机 UA、限速、代理自身反爬 |
| IP 验证耗时 | 异步并发 + 合理超时 |
| 内存增长 | 定期持久化 + 低分清理 |

---

## 八、后续扩展（可选）

- 付费代理接入
- 代理权重轮询（按分数加权）
- Web 管理界面
- 代理质量统计报表
- 分布式部署（多节点协作）

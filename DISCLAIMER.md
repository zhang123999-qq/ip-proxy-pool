# 免责声明 / Disclaimer

> **最后更新**：2026-09-06
> **适用范围**：本仓库全部代码、文档、API、部署实例、公网服务。

---

## TL;DR（一句话）

**本项目是一个学习 / 测试 / 内部工具，使用的是公开免费的代理 IP 源，不保证任何 IP 的可靠性、可用性、质量、匿名性与合规性。生产用请自建代理池或买付费代理服务。**

---

## 1. 关于「代理 IP 源」

本项目采集的代理 IP 全部来自**公开、免费的第三方代理列表 / 公开代理网站**，包括但不限于：

- 公开代理聚合站（如 proxyscrape、openproxylist、proxy-list.download、databay 等）
- 公开代理网站（如 kuaidaili、66ip、89ip、iphai、xiaohuan 等）
- GitHub 上的开源 raw 代理列表仓库（11 个聚合）

**这些源的特点**：
- 大部分是公开爬虫 / 扫描器抓到的「野代理」，少部分是用户自建的代理
- 每天都可能改版、下线、被风控
- 大量代理 IP 由**第三方控制**，其行为本项目无法约束

---

## 2. 不保证事项（按重要性排序）

### 2.1 不保证可靠性（Reliability）

- 单源随时可能 100% 失效、改版、被 Cloudflare 风控、被 IP 黑名单
- 整个池可能因为网络环境、上游限流、源站 DDoS 等原因**整池不可用**
- 本项目已实现「熔断 + 指数退避 + SKIP 降级链」自动应对，但**无法保证 7×24 持续可用**
- 不保证接口响应延迟、采集一轮耗时、验证通过率等任何 SLA

### 2.2 不保证质量（Quality）

- **延迟**：免费代理典型延迟 100ms — 5s+ 不等
- **带宽**：免费代理典型带宽 < 1Mbps，大量并发下极可能阻塞
- **匿名度**：可能存在透明代理、普通匿名代理、高匿代理混在一起（本项目 v3 已加入「透明代理剔除」机制过滤大部分，但仍有残留概率）
- **稳定性**：同一代理可能随时间波动（瞬时能用 / 持续能用 / 5 分钟后挂掉 都常见）
- **地域**：可能不是你想要的地域，可能被目标站识别为代理 IP 直接拒绝

### 2.3 不保证安全性（Security）

- ⚠️ **中间人攻击风险**：免费代理拥有者可以看到、改写你的所有 HTTP/HTTPS 明文流量
- ⚠️ **流量被记录 / 出售**：免费代理拥有者可能记录并出售你的请求内容、Cookie、登录态
- ⚠️ **撞库 / 注入风险**：请求经过第三方服务器，第三方有能力在响应里注入任何内容（包括 JS / HTML）
- ⚠️ **IP 污染**：免费代理 IP 可能已经在各种黑名单里（Spamhaus、目标站封禁、地区限制）
- ⚠️ **DNS 劫持 / 篡改**：HTTPS 证书可能被信任，但下游代理仍能看到 SNI / 域名

**不要用本池访问任何含敏感信息的接口**：
- 不要登录账号（除非你明确知道这个代理没用且无恶意）
- 不要提交密码、Token、API Key、信用卡号
- 不要使用未加密的 HTTP 协议传输敏感数据
- 不要在公司内网 / 受控环境使用（可能泄露内网访问结构）

### 2.4 不保证匿名性（Anonymity）

本池**不承诺提供任何级别的匿名 / 隐私保护**。目标站仍可通过以下方式识别你的真实 IP：

- HTTP header 中的 `X-Forwarded-For`
- TCP 握手的源 IP（如果代理服务器未严格过滤）
- TLS 握手的 client fingerprint
- HTTP/2 连接特征
- JS 端 WebRTC 泄露
- 时序分析

请勿将本池作为「匿名上网工具」或「反追踪工具」使用。

### 2.5 不保证合规性（Compliance）

请遵守以下法律法规与平台规范：

- 《中华人民共和国网络安全法》《数据安全法》《个人信息保护法》
- 《计算机信息系统国际联网管理暂行规定》
- 目标站的 `robots.txt` 与服务条款（ToS）
- 《刑法》第 285 条（非法获取计算机信息系统数据 / 提供侵入工具）

**违规使用场景包括但不限于**：

- 大规模爬取第三方平台数据用于商业用途
- 绕过目标站的反爬 / 访问控制
- 注册大量虚假账号、撞库、刷单
- 绕过地理限制访问被封禁内容
- 任何用于违法 / 灰色行业的用途

### 2.6 不保证来源合法性（Source Legality）

本项目仅作「免费代理列表的采集与验证」工具。**单个代理 IP 的归属、用途、合法性由该代理的提供者负责**。本项目不参与任何代理 IP 的生产、分发、销售、转售。

---

## 3. 推荐使用场景

以下场景**适合**使用本项目：

- ✅ 学习 HTTP 代理池的设计（采集 / 验证 / 评分 / 持久化）
- ✅ 个人测试 / 调试 / 学习爬虫技术
- ✅ 内部工具（爬自己拥有 / 已获授权的数据）
- ✅ 学术研究（非商业用途）
- ✅ 验证自家系统的反爬能力（User-Agent / 速率限制 / 黑名单）
- ✅ 临时性、低并发、非敏感的访问需求

---

## 4. 不推荐使用场景

以下场景**不适合**使用本项目：

- ❌ 生产环境的对外服务（请用付费代理服务）
- ❌ 大规模商业爬取
- ❌ 含登录态 / 账号体系的重要业务
- ❌ 涉及敏感数据（金融、医疗、隐私）
- ❌ 对延迟 / 稳定性有要求（如爬取效率是业务关键路径）
- ❌ 黑灰产 / 违法用途

---

## 5. 生产建议

如果你的业务**真的需要稳定的代理池**，以下方案比本项目更适合生产：

| 服务 / 方案 | 价格区间 | 特点 |
|-----------|---------|------|
| 阿里云 / 腾讯云按量代理 | ¥0.04 / IP 起 | 独享 IP、稳定、合规 |
| ScraperAPI / Oxylabs / Smartproxy | $0.1-$5 / GB | 海外 IP 首选、合规 |
| 自建代理池（拨号 VPS） | 一次性硬件 | 完全自主、性能稳定 |
| Tor 网络 | 免费 | 慢 / 匿名但体验差 |

---

## 6. 责任限制

**使用本项目即视为同意**：

1. 你充分理解免费代理 IP 的风险（见上文 §2）
2. 你将本项目用于**合法用途**
3. 你**自行承担**因使用本项目造成的任何损失
4. 本项目作者不对任何直接 / 间接损失负责（包括但不限于：数据泄露、服务中断、账号被封、法律责任）

---

## 7. 反馈与举报

如果你发现：

- 某个内置代理源涉及违法 / 侵权内容
- 项目文档中的某项说明有误导
- 某个采集 / 解析逻辑有合规问题

请通过 GitHub Issues 反馈，我们会及时处理。

---

*This project is for educational and testing purposes only. The author is not responsible for any misuse.*
